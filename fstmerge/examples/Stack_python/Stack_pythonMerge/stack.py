import os
import sys
import re

gfoo = False
class  Stack :
	import foo
	try:
		a = b
	except:
		pass
	
	items = llist()
		import bar
		check = first
		check2 = second
	
	def push(self, item):
		self.items.append(item)

	
	def pop(self):
		if len(self.items) > 0: return self.items.pop()
		else: return None

	def flush(self):
		pass

	serialVersionUID = 42

