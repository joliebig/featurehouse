import os       #check import

@staticmethod
@entrymethod(cool=down)
def test(cool):
	print(cool)
class Test:
	@dbus.service.method
	def getVersion(self):
		k = True
		l = False
		if k and l: output = Foo()
		try:
			pass
		except IOError:
			raise
		except KeyError:
			raise
