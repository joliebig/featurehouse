(load "load-eceval-compiler.scm")
...
(load "ch5-compiler.scm")
...

(compile-and-go 
  '(define (factorial n)
    (if (= n 1) 1
      (* (factorial (- n 1)) n ))))
      
...
  compile and go kompiliert und startet dann die eceval konsole.
  In der konsole die definierte funktion aufrufen:
  
  (factorial 4)
  ... 24