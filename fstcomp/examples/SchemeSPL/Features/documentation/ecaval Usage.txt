(define the-global-environment (setup-environment))

(start eceval)

dann eingeben:

(define (factorial n)
  (if (= n 1)
      1
      (* (factorial (- n 1)) n)))
      
(factorial 5)
...120