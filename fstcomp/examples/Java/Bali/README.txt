To compile the Bali tools, one library has to be in the classpath: 
jakarta.jar (same directory), for example, 
javac -cp jakarta.jar Bali2JakComp/*.java.

To run a tool, jakarta.jar has to be in the classpath, for example,
cd Bali2JakComp && java -cp .:../jakarta.jar Main.

