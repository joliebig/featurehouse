class not{

    public String toXMLString() {
        //if ( left instanceof bterm )
        //    return "<not>" + left.toXMLString();
        //else
        return "<not>" + left.toXMLString() + "</not>";
    }

}