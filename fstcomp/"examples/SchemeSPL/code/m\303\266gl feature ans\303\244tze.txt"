Regsim.scm

  Funktionen die aus dem Dokument nicht aufgerufen werden:
    get-register-contents
    set-register-contents --macht alles keinen sinn
      die richtigen Registermaschinen-befehle stecken in einer Conditional-abfrage (make-execution-procedure)

eceval
  -gibts nix
  
eceval compiler 
    compile-and-go könnte raus, das kann man vielleicht durch "compile" und dann ausführen ersetzen
    klar, muss gehen
  
    
Ist das hier möglich?:
Feature: interpretierter Code
Feature: compilierter Code
Feature: beides

compillierter Code kann keinen interpretierten Code aufrufen (dazu gibts eine Übung)

Ich glaube der "eceval" ist ein interpreter
"eceval-compiler" ist ein compiler